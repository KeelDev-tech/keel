/data/
__pycache__/
*.pyc
*.pyo
.DS_Store
engines/briefs/

# Internal planning docs with private references — never public
DEPARTMENTS.md
INTEGRATION_PLAN.md
KEEL_DOCTRINE.md
credentials/
# Defense in depth: personalized working copies must never be committable.
# (package.sh already scrubs these from the distributable zip.)
engines/answer_bank.json
engines/employer_form_patterns.json
distribution/
marketing-engine/
launch/muse-campaign-plan.md
launch/hn-warmup-log.md
launch/instagram/

# Marketing/pulse internals — never public
hidden_files/
monitors/
!monitors/
monitors/*
!monitors/formation_writer.py
!monitors/fanout_gate.py
!monitors/pulse_snapshot.py
!monitors/pulse_dashboard.py
!monitors/verify_heartbeat.py

# Internal launch-verification artifact (regenerable; embeds private paths)
launch/proof-points.json
# Funding working material lives in the funding goal workspace, never the public repo
funding/

# Identity boundary (Trent standing rule): worker-charter learning-proposal
# copies can embed real applicant identifiers — the canonical file lives
# privately at job-pipeline/worker-charter/. Never commit keel-tree copies.
worker-charter/learning-proposals.md*

# Identity boundary (Trent standing rule; finding J-20260923-0202-inte-4999):
# untracked generator outputs have embedded real applicant identifiers
# (sanitized 2026-09-22). Regeneration must never be committable.
workbench/
assurance-review-*/
transfer-review-*/
candidates/
exports/

# Repo hygiene (finding J-20260924-0157-inte-5493, deep-sweep run-415):
# untracked bulk scanned clean of real applicant identifiers 2026-09-23
# (versioned 'Wade' strings are the project's own lineage, not PII).
# These are generated/working material, not committable content.
# versioned assurance runner outputs (logs, junit, tests.json) — cf. assurance-review-*/
assurance-checks-*/
# generated source-producer audit artifacts (regenerable)
audit/
# Workstream E canary sources+evidence; commit decision is Trent's/owning arm's — ignored meanwhile
canary/
# versioned external distribution zips (dist/keel-0.1.0.zip stays tracked)
dist/keel-0.2.0-*.zip
# Junk backups and editor litter: never committable
*.bak*
*~
# Department operations workspace: internal standing orders and working
# material, not product content (2026-09-24 triage)
departments/
# Internal account/session operations log (2026-09-24 triage)
launch/hn-warmup-log.md
# Secrets: never committable, even by accident (2026-09-24 repo-wide hardening)
*.pem
*.key
*.p12
*.pfx
.env
.env.*
secrets/
vault/
*token*.json
!**/test_*
