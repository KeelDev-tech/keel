# Keel 0.8 receiving-agent handoff

This patch contains an additive local upstream source system for the 0.7 adapter already integrated on your host. It is not deployed and is not real applicant evidence. The reported 1,010 live tests + 354 subtests and 837 empty leads are receiving-agent status; this release's separate measured results appear in its evidence file.

1. Verify/extract the TXT into a new isolated directory. Read SOURCE_PRODUCERS.md. Run install_source_producers.py against the existing Keel tree without --install first, then apply the additive files after reviewing the exact patch. The installer refuses baseline differences and overwrites. Preserve every existing hold, consent rule, 429 protection, canonical logger and prior package.
2. Run the added tests and synthetic rehearsal in an isolated directory. No model call or rendered browser is needed for these source-layer checks. Discard synthetic records as test data; never promote them to live evidence.
3. Initialize a private source store and register exact identities from the real current flow export. This creates no policy, form, answers, attachments, target, approval or route records by itself.
4. Wire the actual host events that produce those records: active policy change, actual form capture, supported answer selection or human input, document generation/capture, observed posting identity, and actual account/route selection. Preserve true source references, versions, observation times and validity. Use expected-generation compare-and-swap for updates. Follow SOURCE_CAPTURE.md; unsupported semantics stay blocked.
5. Prove one real role end to end. Six valid sources permit an explicit review request. A real authorized human decides; the host records that decision. Missing data alone never becomes a human request. A genuine approval binds all six revisions. No approval or source-authenticity evidence is invented by this package.
6. Consume producer-export.snapshot with the installed0.7 mapping adapter, or join rows[].columns by the complete scope tuple. Preserve exact current-flow identity/digest binding. Rebuild truthful assurance envelopes and run all existing branches conjunctively. No canonical write is performed by the patch exporter itself.
7. Run doctor on the actual host. Report hardware/dependencies independently of real model inference and browser rendering. Install suitable licensed model weights and free local browser dependencies only through the host's normal explicit setup. Do not claim tests that were not performed.

Return RECEIVED, VERIFIED, INTEGRATED, SHADOW_VALIDATED and DEPLOYED separately, with counts for real source coverage, actual requests, genuine decisions, and remaining source/semantic/assurance/trust holds. Do not call source completeness production readiness. Real deployment and external submissions remain outside this additive patch.
