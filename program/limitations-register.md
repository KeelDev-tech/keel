# Keel Limitations Register

**Owner:** Keel Program Office. **Standing:** evidence before claims. Every entry names limitation, impact, owning department, status, and the path to clear. P0 = blocks unattended side effects or a market claim until closed; P1 = must resolve before claiming a dependable production service; P2 = experience/maintainability.

**Sources:** `~/workspace/user/files/System_Audit_and_Improvement_Roadmap_2026-09-17_1_1m6x.md` (46 findings, 2026-09-17), `~/workspace/user/files/Market_Analysis_and_Competitive_Build_Plan_2026-09-17_6_eu68.md` (2026-09-17), `~/workspace/user/files/Submission_Integrity_Review_and_Next_Steps_2026-09-17_8_0fvt.md`, `~/workspace/user/files/READY_Pool_Competitive_Build_Plan_2026-09-17_4_eo30.md`, `~/workspace/user/files/gap-register_10_2bjj.json` (G1–G8, as_of 2026-09-17, production_deployed: false), `~/AGENTS.md` operating lessons, `~/workspace/keel-transfer/conflict-review.md`, live pipeline pulse 318 (2026-09-17 19:04Z), lane watchdog (12:05 PDT), api-direct board stats (counted ~12:15 PDT).

**Grounding rule:** every entry traces to a named source. Where sources disagree, both are cited. Nothing invented.

**Live snapshot (2026-09-17):** ledger submitted 195 (a prior pulse reported 196 — ledger semantics unreconciled); standard queue 2,717 · needs_input 148 · strategic 150; READY 6 (drained 47→6; earlier trajectory 91→0→51→5/6) · strategic READY/firing 0; effective launchable pool 0 (all six READY held by fresh api-direct refuse-locks); queue IN-FLIGHT: one Stripe role (J8029043); rolling submission rate 1.0/hour; browser idle 9.3 min; stalled=false; verification promotion 65.1%; enrichment 100%; RL-2 supply:burn 0.37 < 1.0 (lane expansion frozen); RL-3 verification backlog 607; OpenAI GPT path 429 insufficient_quota (GPT red-team/polish unavailable).

---

## A. P0 — submission truth (blocks unattended side effects or market claims)

| ID | Limitation | Impact | Owner | Status | Path to clear |
|---|---|---|---|---|---|
| F17 | HTTP 2xx treated as proven application submission on direct-submit paths | False success counts corrupt measurement; believed-sent applications may never have arrived | Keel Build | Open (partially mitigated in review copy; no validated success-receipt parser exists) | Provider/attempt-correlated confirmation contract; ambiguous 2xx stays UNKNOWN with durable reconciliation hold |
| F18 | Central outcome recording bypasses the evidence gate | Unproved "submitted" claims can enter ledger/learning/telemetry, inflating reported submissions | Keel Build | Open (text gate + hold enforcement added in review copy; provenance open) | Final writer accepts only a verified receipt reference from a trusted verifier, not a caller-controlled flag or arbitrary note |
| F20 | Approval fingerprints bind resume filename, not file bytes | Same-name attachment replacement changes what is uploaded after approval | Keel Build | Open | Immutable submission bundles (attachment SHA-256/size/type, target identity, exact answers, form version); upload the approved bytes |
| F21 | Human approval is a caller-supplied label | A worker can self-authorize by naming the operator; claimed human exemptions unverifiable | Keel Build | Open | Authenticated approval records bound to principal, scope, expiry, exact question/bundle, immutable evidence |
| F22 | Ambiguous API outcomes fall back to a second (browser) submission channel | Automatic duplicate-submission risk on uncertainty — candidate-reputation and employer-relationship risk | Keel Build | Open (partially fixed on covered API paths in review copy; browser-origin uncertainty + cross-role identity open) | One durable attempt shared by API and browser lanes (browser intent before click; canonical cross-role identity); no retry/transport switch until authoritative reconciliation |
| F34 | Answer-bank scope metadata dropped before use in application briefs | A scoped consent/essay answer for employer A can appear as an authorized answer in employer B's packet | Keel Build | Open | One context-aware answer resolver used by every consumer — value plus scope/source/expiry; abstain on inapplicable scope |
| A-B1 | No validated success-receipt parser exists for API paths | Accepted submissions stuck UNKNOWN cannot be counted, reconciled, or retried safely | Keel Build | Open | Receipt verifier — provider-specific evidence contract per route; persist raw receipt artifacts and digests with request/attempt correlation |
| A-B2 | Holds keyed on role_id, not canonical application identity | Different role IDs for the same logical job evade the local duplicate guard | Keel Build | Open | Deduplicate by canonical application identity rather than a mutable queue role ID |
| A-B3 | Browser clicks do not create durable intents | Browser-origin uncertainty not covered by the attempt-hold system; retry risk in the browser lane | Keel Build | Open | Commit a durable intent immediately before a browser submit action; browser claim-refusal logic tested |
| A-B4 | Historical attempts not automatically imported | Migration or restart can duplicate previously ambiguous attempts | Keel Build | Open | Reconcile historical ambiguous attempts before enabling new paths; do not use DB deletion, new role IDs, or lease expiry to release a hold |
| A-B5 | No authenticated reconciliation mechanism for holds | UNKNOWN holds accumulate; lease expiry does not clear them; no sanctioned resolution path | Keel Build | Open | Authenticated reconciliation preserving evidence and history when resolving a hold |
| A-B6 | No atomicity across queue, ledger, technique library, telemetry | Lost update or partial outcome write possible — claimed vs recorded vs learned diverge | Keel Build | Open | One transactional state boundary for claim, attempt, outcome, event changes |
| A-G0 | Design-level product gaps G-1..G-10 from comparator design teardowns (2026-09-19, intel/comparator-design-teardowns.md): no user-facing recurring machine; no execution escape-hatch surface; blocker resolution invisible as product surface; pipeline ends at submission while competitors' continue to interview; no productized cost story; discovery breadth not productized; volume controls are operator levers not user policy; no user-facing outcome feedback loop; distribution deliberately deferred; privacy architecture unclaimed by anyone. White space confirmed (not assumed): nobody advertises submission receipts, answer provenance, constraint enforcement, or outcome cohorts | We hold the integrity white space but ship no product surface that shows it | Keel Launch & Growth / Keel Build | Open — teardowns done, spec-matrix §5 updated; gaps flow into LG-04/LG-03 positioning and Build surfaces. Comparator trials (T4) still blocked on Trent's spend word | Productize the recurring machine, escape-hatch surface, visible blocker-resolution surface, post-submission pipeline, cost story (blocked on T3), privacy-architecture claim |
| A-B7 | Greenhouse frontend route used by code is reverse-engineered and unvalidated | Direct-submit path may rely on an unauthorized or fragile route | Keel Build | Open — audit 2026-09-17 measured it: employer-hosted Greenhouse renderer is structurally DEAD over HTTP (40 technique_blocked, byte-identical signature, 0 successes in 3 days telemetry); the same postings are LIVE and browser-viable (3/36 submitted via browser with confirmation_text). Renderer verdict detail was discarded at verify time, so blocks fired at attempt time after full verify→READY→packet→launch investment. **P1 SHIPPED + LIVE 2026-09-17 12:59 PDT (PQ-9):** full `api_direct_detect` verdict (renderer/reason/detected_at) stamped at verify time; employer-hosted short-circuits before the api-direct guard lock (no lock, no packet, no fastlane screen); re-stamped every verify, no pinning; missing verdict falls through fail-safe. 163 tests green (6 new + 157 neighboring); ARM 70 seam review approved; `renderer_employer_hosted` registered in GATE_TYPES — gate emits with no unknown-value warning, 6/6 renderer tests green. The ~48 attempt-time blocks/day are now short-circuited before guard. Renderer-audit arc closed on the P1 half. Entry stays open for per-employer contract validation (P2, Trent-gated PD-5). conflict-review.md §1 premise corrected with dated note | Persist the full api_direct_detect verdict (renderer, reason) on queue entries at verify time; route employer-hosted straight to browser; per-employer embed-endpoint fallback contract (P2, Trent-gated) |
| A-B8 | Native rejection allowlist derives from implementation and synthetic tests, not live-provider validation | Real provider rejection semantics could be misclassified — wrong fallback or blocked legitimate retry | Keel Build | Under review | Validate or tighten the allowlist against live providers before production enablement |
| A-B9 | Caller-supplied confirmation language still not authenticated provider evidence | F18 only partially mitigated — the evidence gate screens text, it does not authenticate receipts | Keel Build | Partially fixed | Trusted capture/verifier that attests to provider evidence |
| A-C1 | Api-direct kill switch (keel_legacy_hold.require_gateway()) — REJECTED | Inserting it kills the live api-direct transport; the "wire the Keel gateway" path does not exist in the live repo | Keel Build | Rejected — never apply | None; working lane stays. Any gateway design needs an explicit architectural decision first |
| A-C2 | Receipt-writer adversarial review returned FAIL | The review-copy keel_core.store.Store receipt writer cannot be trusted as the outcome authority | Keel Build | FAIL — review copy only, NOT deployed, no-go for cutover | Adversarial hold clears only after a rebuilt, independently re-verified writer |
| A-C3 | Two competing receipt-integrity architectures (live F18 vs patch's separate-SQLite store) | Cannot coexist in record(); the patch model conflicts with live F17/F18/F20/F22 evidence, intent, correlation, UNKNOWN handling | Keel Build | Held — no separate SQLite authority without reconciliation with queues, locks, intent, telemetry | Keep F18; shadow-only reconciliation design; never fabricate/backfill receipts; never treat HTTP 2xx or generic confirmation as submission proof; historical uncorrelated records stay LEGACY_UNVERIFIED |
| A-C4 | Transferred brief language contradicts standing directives — REJECTED | "PREPARATION ONLY / recorded authority" wording would revoke Trent-authorized standing behavior (account creation, CAPTCHA auto-solve, EEO handling, market-midpoint compensation) | Keel Build | Rejected | None — live brief_builder language stands |
| A-C5 | Transferred verify_retry.py is stale — SUPERSEDED | Live RP01 (2026-09-17) supersedes the patch's hunks more robustly (safe-mode fallback vs hard-False refusal); adopting would be a regression | Keel Build | Superseded — do not apply | None |
| A-C6 | Transferred 344-test claim independently unreproduced | No third party can verify the transfer's test claim | Keel Build | Open — awaiting the original 537-file source tree/source ZIP | Obtain source export; reproduce before relying on the claim |

## B. P0 — READY supply & lock layer (the proving ground is starved)

| ID | Limitation | Impact | Owner | Status | Path to clear |
|---|---|---|---|---|---|
| B-E1 | Api-direct loop acquires launch locks then refuses them all as `fastlane_browser` — stale 2h locks; `--spawn` refuses "launch lock not acquired: HELD" (Stripe YC J8181026, SDR AUNZ J8074892, Tech Ops J7718947 on 2026-09-17; ~42–44 stale Stripe locks in pulse 317) | Refill blocked at spawn time | Keel Pipeline Performance / Keel Build | Open — manual refill procedure exists (check run log for `refused / fastlane_browser`, release stale lock to _released/, re-spawn). Release-on-refuse proposal unshipped; Trent skipped the proposed tap on 2026-09-17 — proceed via autonomous engine design, not repeated prompting | Release-on-refuse wired as engine behavior (lock released when the acquiring loop itself refuses), with tests |
| B-E2 | 86 api-direct `no_packet` skips | Packetless-but-valid leads die as scan refusals instead of flowing to packet build or the browser lane | Keel Pipeline Performance | Open | Route no_packet refusals into packet-build (stage-time packet gate) or browser-lane handoff instead of terminal refuse |
| B-E3 | Launch-guard stand-down telemetry truncated to `'}'` | Correct HELD: STAND_DOWN decisions (4 observed: Coinbase J8114684 ×3, Stripe J7277406 ×1) unreadable in the log; cannot audit why a launch was held | Keel Pipeline Performance | Open — proposal in pulse finding J-20260916-2050-gate-930 | Parse verdict JSON in guard() to emit status+holder |
| B-F1 | READY drained 47→6 with zero refill; effective launchable pool 0 (all six held by refuse-locks); rolling submission rate 1.0/hour | The lane cannot launch | Keel Pipeline Performance | Open | Refill from clean verified sources (discovery → dedupe → verification), sanctioned lock cleanup, B-E1/B-E2 fixes |
| B-F2 | No discovery sweeps for ~36 hours | Feeder dry — discovery isn't producing URL-bearing leads; RL-2 supply:burn 0.37 froze lane expansion | Keel Pipeline Performance | Open | Resume discovery cadence; per-source yield tracking (source_yield_proposals.py) doubles down on high-yield sources |
| B-F3 | No-AI/unaided-work attestation wave spreading cross-employer (176 events 2026-09-17: Stripe 97, Databricks 22, Samsara 20; Fastly, Salesloft, MongoDB, Elastic newly observed) | Single largest parking pressure on genuinely attestable-but-false leads (Trent's law: never pre-authorize — not a bug, a boundary) | Keel Pipeline Performance | Open by design | Bank employer-scoped Trent-words answers where he gives them (tray_answer compounding repair path); fingerprint-registry proposal is banked only, not built; surface with scoped facts if volume keeps bleeding |
| B-F4 | Greenhouse Enterprise wall — nearly all job-boards.greenhouse.io boards render the Enterprise endpoint (GitLab, Anthropic, xAI, Neo4j, AIO Logic refused); ledger shows zero api-direct production submissions ever | Api-direct fast lane has almost no live candidates; throughput multiplier theoretical for Greenhouse | Keel Pipeline Performance | Open — structural | Only older boards.greenhouse.io embeds viable (detector requires the form blob; securly13 mirror closed) unless new direct-submittable boards appear |
| B-F5 | Lever direct-submit refused by design over pure HTTP (API-key wall + platform-wide hCaptcha); lever_submit.py dry-run pre-flight only | Lever leads stay browser-path | Keel Pipeline Performance | Open | Live enablement requires supervised evidence, never a code change alone |
| B-F6 | Ashby is browser_only in the ATS radar (not HTTP-renderable) | No HTTP transport for Ashby postings (e.g., Clipboard duplicate-submit platform) | Keel Pipeline Performance | Open — structural | Browser path only (duplicate-submission risk managed by single-driver rule + launch lock) |
| B-F7 | Workday UNTESTABLE — no sample URLs | Workday transport capability unknown; every Workday lead a blind spot | Keel Pipeline Performance | Open | Acquire real Workday sample posting URLs and probe |
| B-F8 | Feeder watchdog found a dry pool (2026-09-15 first run: 25 parked leads all "NO URL", 0 promoted) | Feeder cannot manufacture supply from a URL-less pool (premise later corrected: true pending-verification URL-less share is 0.2% — the real gap is net-new URL-bearing discovery) | Keel Pipeline Performance | Open | Net-new URL-bearing discovery; URL-health reads must count application_url\|apply_url\|posting_url |
| B-F9 | Buffer-queue drift — a re-screen parked 4 Databricks leads whose fresh launchable packets sat stale in buffer/ | Lane could spawn from a buffered packet whose entry is no longer READY/IN-FLIGHT | Keel Build | Partially fixed (rule: buffer-queue consistency sweep after ANY park/purge; canonical state is the queue) — no automated sweep ships the rule | Automated consistency sweep |
| B-F10 | RP01 intake/policy mismatch had starved high-priority leads (PRIORITY APPLY accepted at intake but not by verification/promotion/block logic; 168-hour cooldown) | High-fit, high-priority leads lost to a weekly cooldown | Keel Pipeline Performance | Cleared live 2026-09-17 (RP01 in production) | Done — keep band-policy consumers wired to the canonical set |
| B-D2 | READY is a label that can sit on blocked work | Dashboard READY counts mislead — nominal READY can exceed immediately launchable inventory | Keel Pipeline Performance | Open | One authoritative evaluate_readiness(lead, context) consumed by intake, verification, preparation, dashboard, claims; report nominal-to-actual loss |
| B-D3 | "Promoted" does not mean "passed the complete readiness contract" | Promoted leads fail downstream; preparation spend wasted; execution starved | Keel Pipeline Performance | Open | Shared readiness contract — posting/employer identity, policy version, answer provenance, attachment digests, no prior attempt/hold |
| B-D4 | Fixed buffer constants (in-flight cap 4, packet target 4, ceiling 8, age cap 12h) not demand-driven | Starved engine cannot recover to target; buffers cannot absorb bursts | Keel Pipeline Performance | Open | Adaptive watermarks from measured P95 preparation latency plus burst allowance; subtract work in progress |
| B-D5 | Refresh-skip watermark hashes queues but not answer/policy/form/attachment dependencies | Changed answers/materials/policy can leave stale packets launchable; a changed resume could ship against an old approval | Keel Pipeline Performance | Open | Readiness invalidated on dependency changes, not just queue changes or elapsed time |
| B-D6 | Event-driven top-ups only fire when refresh_buffer() runs; external scheduling hooks not fully supplied | Buffer may sit empty while supply exists; controller not continuously responsive | Keel Pipeline Performance | Under review (external evidence missing) | Durable event stream/outbox waking the controller after claims, invalidations, answer updates, verification results, source arrivals; timed reconciliation to recover lost wake-ups |
| B-D7 | Starved engine's observed launch rate is censored demand | Zero-demand collapse — the system stops trying precisely when it is starving | Keel Pipeline Performance | Open | Determine demand from approved active windows, permitted slots, measured non-starved capacity; never reduce refill target to zero because the engine has been idle |
| B-D8 | Throughput ceiling implied by verify yield (~200 verifications/hour at 0.056 net yield caps promotions at ~11.2/hour; a 30/hour target needs ≥15% yield) | Raising queue targets cannot solve a conversion bottleneck; throughput ambitions unfunded | Keel Pipeline Performance | Open | Fix conversion and wasted checks first (RP01, inventory audit, blocker-family repair, source reallocation); verify the metric definition before capacity planning |
| B-D9 | No shared readiness evaluator and no adaptive controller exist (designed, not built) | Prepared vs executable inventory conflated; shortage causes unexplained; refill unmanaged | Keel Pipeline Performance | Under review (designed, not built) | Implement evaluate_readiness plus dependency invalidation; adaptive controller in shadow mode first, replaying source outages/bursts/low-yield scenarios |
| B-D10 | Dashboard shows READY counts but not executable READY by route, runway, replenishment ETA, per-stage yield, nominal-to-actual loss, or typed shortage causes (SUPPLY_LIMITED / PROVIDER_LIMITED / AWAITING_DECISION / INTERNAL_FAILURE) | Operators cannot see or act on the real bottleneck | Keel Pipeline Performance | Open | Dashboard per the READY spec separating scheduled pauses, provider limitations, genuine decision waits, market exhaustion, internal starvation |
| B-D11 | No guard against dividing by near-zero yield | Unbounded wasted exploration when supply is truly exhausted | Keel Pipeline Performance | Open | Declare the supply gap and run bounded diagnostic/exploration work; declare infeasibility explicitly rather than silently clamping |

## C. P1 — trust perimeter (resolve before production claims)

| ID | Limitation | Impact | Owner | Status | Path to clear |
|---|---|---|---|---|---|
| F27 | Local storage has no single durable transaction boundary (rename without fsync; queue/ledger/learning/event updates span files) | Partial writes corrupt state; committed authority can be lost on crash/power loss | Keel Build | Open | Move canonical workflow state and event acceptance into one local transactional database; choose durability settings from the agreed recovery objective and measure latency |
| F28 | Audit-chain integrity weaker than its assurance claims (process-local lock, head anchor beside the writable log, append failures degrade to unchained writes, MUSE audit rows mutable) | Tamper/rewrite of the log plus anchor is not independently detectable; "immutable ledger" unearned | Keel Build | Open | Serialize events transactionally with sequence numbers and durable signed checkpoints under separate authority; expose degraded audit health |
| F29 | Tenant scoping stops short of the whole control plane (login searches usernames globally; workspace config process-global) | Hosted expansion risks cross-tenant identity/config crossover | Keel Build | Open | Tenant-qualified login and per-tenant configuration, or document single-tenant-only deployment |
| F30 | First-run setup is not an atomic recoverable operation (setup token consumed before validation; concurrent setups unprotected) | Partially committed setup; token loss leaves an unusable setup path | Keel Build | Open | Validate first; atomically claim setup; explicit setup state; exactly-one owner |
| F31 | Local HTTP is not a production web perimeter (no Host/Origin/CSRF enforcement, no bounded read timeout, exceptions expose internal messages) | Hosted expansion unsafe without a real deployment boundary | Keel Build | Open | Maintained server/reverse proxy, TLS, strict trusted-host/origin, CSRF, bounded body/read/concurrency, sanitized errors |
| F32 | Key and secret isolation limited to one local process identity (services share one managed signing key store; connector secrets in ordinary config rows) | A draft worker could obtain the release signer or connector credentials | Keel Build | Open | Secret-provider interface, interprocess-safe atomic key rotation, separate signing capabilities per service |
| F33 | Deletion and export report more completeness than they deliver (deletion leaves derivatives; traversals capped at 200 rows; export silently caps tables at 1,000 rows) | "Can I trust you with my information?" cannot be answered truthfully; export/deletion claims unverifiable | Keel Build | Open | Paginated dependency walk with per-store counts/failures; complete paginated export with checksums and explicit exclusions |
| F38 | Agent safety is largely prompt-level; no end-to-end threat evaluation | No proven resistance to hostile job pages, poisoned evidence, multilingual instructions, tool-result injection | Keel Build | Under review (design gap) | Treat web pages/files/model output as untrusted data; restrict capability/egress by code; validate tool arguments independently; held-out attack corpus across the full workflow |
| F39 | Operations-control claims not verifiable from documentation alone | Cannot evidence that operational controls exist or work | Keel Build | Under review (external evidence missing) | Export sanitized monitor code, configuration hashes, execution evidence; controlled failure drills trigger expected alerts; restoration works from verified backup |
| F42 | Federated telemetry suppression does not establish privacy (arbitrary ATS/gate strings survive as bucket labels; repeated events from one lead can meet the threshold) | PII-bearing labels could export; "anonymized" claim false. Data Profit Pipeline absolute G1 privacy-counsel gate still required before publication/offering/pricing/sale | Keel Build | Open | Allowlist categories, bound contributions, deduplicate units, define the privacy unit, test differencing across exports |
| F43 | Outbound fetch/upload boundaries need a dedicated security review (pipeline follows URLs/redirects; no complete redirect/DNS/private-address/protocol/credential policy demonstrated; no SSRF probe run) | Credentials or applicant data could be moved to an unapproved destination via crafted job page | Keel Build | Under review (static risk, not exploited) | Retrieval and side effects through a broker with separate permissions, allowlisted providers/operations, redirect revalidation, private-address blocking, bounded payload/time budgets |
| F19 | Worker ingestion is not replay safe | Replaying the same envelope can duplicate learning evidence and telemetry | Keel Build | Open | Assign attempt_id and event_id before execution; enforce unique receipt/event records transactionally |
| F23 | Publishing authorization does not bind the effective connector account | Approved account A can be silently swapped to account B before dispatch — trust blocker for any multi-account release claim | Keel Build | Open | Bind connector_id, immutable provider account ID, verified configuration revision into the release manifest; check at dispatch and reconciliation |
| F24 | Media bytes, rights and inspection provenance not authoritative (signing a media path does not freeze file bytes; rights default to cleared/owned) | The reviewed asset is not provably the published asset | Keel Build | Open | Immutable content-addressed media, explicit rights records, reviewer/inspector evidence; authorize the complete outbound caption+asset bundle |
| F25 | Policy checks and capacity checks are not atomic with dispatch admission | Quota overruns; policy changes between check and handoff | Keel Build | Open | Reserve capacity with the claim in a short transaction; recheck the policy epoch at the egress boundary |
| F26 | Process-death recovery and bounded retries incomplete (no claim lease/heartbeat recovery; outbox has no backoff schedule) | Items stranded without supported recovery state; blind recovery risks duplicates | Keel Build | Open | Worker lease/attempt timestamps, recovery to UNKNOWN, tri-state reconciliation, retry-after/backoff/jitter, inspectable dead-letter workflow; never recover by blindly resetting to QUEUED |
| F35 | Export sanitization assertions not supported by the archive (excluded runtime files and operational records present despite exclusion claims) | Unsafe redistribution risk; leak-audit claims are historical, not renewed | Keel Build | Open | Rebuild with an allowlist; exclude runtime state/backups; scan the final ZIP including metadata/images; generate the audit from actual results |
| F36 | Advertised offline test baseline not reproducible (stdlib-only instructions conflict with pytest imports; an executor run made an external api.ashbyhq.com request) | Test claims cannot be reproduced by a third party; the offline guarantee unverified | Keel Build | Open | Pin development dependencies; one documented runner; block egress including proxy tunneling before tests; separate live contract tests |
| F37 | Schemas and compatibility versions not consistently enforced (default schema_version without version check; bool accepted as int; multiple status/queue vocabularies need regex bridges) | Malformed statuses/NaN/Infinity can become trusted state or counted as success | Keel Build | Open | Versioned schemas, exact primitive checks, finite scores/ranges, explicit upgrade adapters |
| F46 | Production reliability and comparative superiority unproved (no production connector credentials, deployment, observed SLO window, restore evidence, load profile, or independent competitor benchmark) | "Best in the world" claim has zero evidence base | Keel Competitive Intel | Under review (external evidence missing) | Close remaining P0s, then contract tests, restore/fault drills, shadow operation, small authorized canary, independent benchmark with matched tasks/budgets/restrictions |
| C-G8 | Privacy and operations (gap-register G8 — LIMITED_LOCAL_ISOLATION_TESTS) | Production perimeter, two-user isolation, adversarial egress, backup restore, complete export/deletion accounting all unverified | Keel Build | Partial | Production perimeter and service capabilities; two-user hosted isolation and adversarial egress tests; backup restore with observed recovery objectives; complete export/deletion accounting |

## D. P2 — pipeline operational defects (from AGENTS.md — open)

| ID | Limitation | Impact | Owner | Status | Path to clear |
|---|---|---|---|---|---|
| D-H1 | Verify/score reads the FORM, not the POSTING — 4 posting-text misses in one night (Addepar 5-day on-site submitted, HelloFresh bachelor's-required submitted, Lucid 50% travel launched pre-fill, Giga full-time on-site) | Post-submit D1 flags; burned browser-lane slots; submissions that cannot survive the employer's first screen | Keel Build | Open | Posting-text extraction pass (travel %, office days/week, degree requirements) at verify/score time; park violations before brief_build; browser lane is the last line of defense, not the screen |
| D-H2 | Spawn briefs never enumerate the attestation scope — the five pre-authorized attestations live in answer_bank/prescreen but not in the spawn brief's truthful-values section (a LeanTaaS session improvised a vaccination-policy acknowledgment) | Browser improvises on acknowledgment/attestation checkboxes | Keel Build | Open | Spawn brief carries the explicit attestation scope — only the five pre-authorized keys (arbitration, background check, at-will, information truthfulness, data privacy) may be checked; any other acknowledgment parks as Trent-only |
| D-H3 | Experience-assertion checkboxes can be filled from employer-name inference (a CSSMER session checked four boxes citing a "verified Napa River Inn hospitality ops record" not in answer_bank) | Invention risk on every checkbox/radio form | Keel Build | Open — three compound fixes listed, none shipped | (1) form-intel field-type mismatches caught at launch; (2) experience-assertion checkbox/radio without a banked verified fact parks as Trent-only; (3) park entries quote the TRUE blockers |
| D-H4 | SMS consent overgeneralization — bare answers.sms_texting_permission="Yes" rendered into every launch brief | Live launches carried an overgeneralized consent value until a supervisor OVERRIDE forced parking on any SMS/WhatsApp consent question | Keel Build | Partially fixed — standing global answer quarantined to _quarantined; interim OVERRIDE needed per launch until per-employer consent handling is wired | Per-employer consent never promoted to global without Trent's explicit scoped authorization |
| D-H5 | Cost model v1 leaves cost_http_requests and cost_taps UNMETERED per submission | Composite cost_units exclude HTTP/tap dimensions — cost-per-submission trend partial; blocks publishing cost per verified eligible completion | Keel Build | Open | Per-submission HTTP attribution and a tap signal (browser_tasks is a proxy only, never recorded as taps) |
| D-H6 | Ledger headline gaps — resume_lane missing on 86%, date_submitted missing on 86% of SUBMITTED rows | Lane conversion analytics stay thin until populated going forward | Keel Pipeline Performance | Open | Populate both fields on new writes going forward (additive-only stamping via canonical append path) |
| D-H7 | Zero browser lifecycle telemetry — browser subagents have no shell, so STEP 7 log_event.py events cannot run there; the task's "continue anyway" rule keeps the application going with zero emitted events | Compliance metrics depending on those events (e.g., lifecycle-compliance) undercount browser-path launches | Keel Build | Open | Emit the events loop-side from the parent agent instead of relying on the browser subagent |
| D-H8 | FIM watches goal-owned cron defs on disk, but a schedule vanishing from the scheduler does not alert | The unsanctioned-deletion class can recur silently at the scheduler layer | Keel Build | Open | Wire cron_drift.py into Tier-1 so scheduler-side vanishings alert |
| D-H9 | pdt_now() helper for queue_io.py proposed but never added | Ad-hoc lane writers can reintroduce the UTC-labeled-PDT timestamp bug; future-dated stamps delay the feeder watchdog's >2h stale-IN-FLIGHT flag | Keel Build | Open — meanwhile every ad-hoc writer uses the ZoneInfo pattern explicitly | Engine arm adds the helper |
| D-H10 | Mining proposals die unreviewed — "an unreviewed proposal file is a broken loop" | Banked answers never land; learnings don't compound | Keel Program Office | Open | Main-agent review discipline on every mining-arm handoff |
| D-H11 | Orphaned parked leads — a park without a queue entry + reason is a lost lead (one needed hand-recovery) | Any non-canonical park path can silently lose leads | Keel Build | Partially fixed — rule in place (canonical park writes queue entry + reason); incident lead restored fail-closed | Canonical park path enforced in code, not just convention |
| D-H12 | Residuals: queue_io.queue_lock() needs a configurable path for test pipelines; log_event gate-vocab test failure is pre-existing and unrelated to shipped fixes | Test pipelines can't point at their own lock file; one standing test failure | Keel Build | Open | Add the parameter; fix or quarantine the pre-existing test |
| D-H13 | C-14 charter amendment (canonical-counting evidence rule) — APPROVED by Trent 2026-09-17 | Promoted 2026-09-17: exact package wording is now canonical charter constraint C-14 in `worker-charter/assemble_charter.py` (CRON_EXTRA); charter assembly validated, zero placeholders | Keel Program Office | **CLEARED 2026-09-17** — human approval recorded in `worker-charter/promoted_outcomes.json`; same-rule ID collision resolved by in-place upgrade (no duplicate C-21) | None — closed |
| D-H14 | parking_schema note_required false positive when queue_notes is list-typed — potential affected: 1,335 standard + 103 needs-input records | False parking pressure on correctly-annotated records | Keel Build | Open — proposal unshipped (triaged under "tested value only") | Ship the schema correction after test evidence |
| D-EV3 | ~36h unwired sweep gap — an ATS-radar onboarding proposal sat unacted 37h (coinbase gh_jid-proxy case); learning-proposals triage backlog 135 drafts vs weekly evaluator | Patterns keep costing the lane for days before constraints land; the next gh_jid-proxy employer repeats the miss | Keel Pipeline Performance / Keel Build | Partially closed 2026-09-17 — triage sieve shipped (`worker-charter/triage_sieve.py`, 18/18 tests green, neighboring 39/39): 136 drafts → 73 promote-candidates, 45 review, 14 needs-evidence, 4 duplicate, 0 defer (fail-closed). Tool never promotes (verified behaviorally + statically); promotions stay human-approved. Open: wire digest into evaluator prompt context (coordinator's call); ATS-radar onboarding (coinbase) still open | Wire digest into evaluator context; blackboard claim for ATS-radar onboarding |
| D-E4 | Browser task slot starvation pattern — needs_user sessions in parked states hold Chromium slots indefinitely while live work queues (2026-09-15: ten parked tasks starved one live application ~15 min) | Lane stalls despite READY work waiting | Keel Build | Partially closed (lane-stall post-mortem: parked-forever sessions always propose close; repair-inflight with fail-closed guards; cron delivers to main chat) — rule: parked task ≠ live session | Keep the parked_task_sweep discipline; never-halt close applies only to lane-routable pipeline blockers (carve-out: user-takeover steps and same-day deadline work) |

## E. P2 — measurement, design, and user-journey gaps

| ID | Limitation | Impact | Owner | Status | Path to clear |
|---|---|---|---|---|---|
| F40 | Best-in-world bars reward volume and backlog over verified value (permanent 4× supply/burn target grows backlog; raw submissions/hour rewards duplicates, low fit, weak confirmation) | Chasing throughput metrics degrades trustworthy completions | Keel Competitive Intel | Under review (design gap) | Optimize qualified verified outcomes per dollar and operator minute under hard integrity constraints; control buffer coverage and queue age rather than unbounded intake |
| F41 | Learning signals are observational and may be premature (small-sample ranking differences against a pooled baseline; response delays and selection effects confound apparent gains) | "Improvements" from the learning loop may be illusory; compounding on noise | Keel Competitive Intel | Under review (design gap) | Mature cohorts, censoring-aware time-to-response analysis, calibration and uncertainty intervals; pre-register genuine randomized treatments where permitted |
| F44 | Large orchestrators and hardcoded paths obstruct reliable scaling (three central modules total 8,497 lines; 141 files with tilde-workspace/expanduser references) | Regression and portability risk; two instances cannot run isolated | Keel Build | Under review (design gap) | Explicit runtime context; extract state transitions, transport, evidence, policy, persistence behind small interfaces; contract tests before optimizing concurrency |
| F45 | Full user journey and accessibility unverified (keyboard access, mobile layout, assistive tech, setup recovery, second-operator end-to-end use never tested) | No evidence a new user can operate the product; "can I use this without operating the machinery?" unanswerable | Keel Launch & Growth | Under review (external evidence missing) | Keyboard/screen-reader/responsive acceptance tests plus independent operator walkthrough using only documented onboarding; target 8/10 users to first approved packet unaided in 15 minutes |
| E-I6 | Cost instrumentation is partial (some measures are proxies, composite units are not actual dollars; browser launches used as an approval-effort proxy) | Cannot publish cost per verified eligible completion; unit economics unknown; pricing decisions unfounded | Keel Program Office | Open | Meter model, browser, network, failed attempts, infrastructure, and human recovery cost against stable attempt IDs; publish cost per verified eligible completion and per active user minute including failures and missing-meter coverage |
| E-I7 | Fit scores and observational learning do not prove improved interviews (no independent relevance labels, job-age checks, or consent-based outcome ingestion) | Cannot answer "will I get better results?"; relevance claims are marketing, not evidence | Keel Competitive Intel | Open | Independent relevance labels, job-age checks, outcome ingestion with consent, mature cohorts, controlled changes; report calibration, response, and interview rates with cohort age, denominators, and uncertainty |
| E-I8 | Product boundary ambiguity — public-edition onboarding docs stop at a launch packet while the uploaded system contains private execution paths | Unclear which edition a customer receives — mis-set expectations; commercial risk from promising execution the edition doesn't deliver | Keel Program Office | Open | State explicitly which edition a customer receives |
| E-I9 | Comparator identities left as unnamed A/B/C placeholders; build-roadmap.json and benchmark-contract.json not found in the delivered package | Competitive-evidence track missing its machine-readable contract artifact | Keel Competitive Intel | Open | Reproduce the named benchmark contract and build-roadmap files with audit IDs, dependencies, and acceptance tests; define the three products' versions/plans before testing |
| E-I10 | No measured provider-and-form support matrix — connector code exists but supported-route coverage and live reliability unverified | Cannot promise which jobs the system can actually apply to; users hit unsupported routes blind | Keel Build | Open | Measured provider-and-form support matrix, targeted source freshness, contract fixtures, browser handoff for unsupported routes; report eligible-opportunity coverage and completion by route including exclusions and account requirements |
| E-I11 | Pricing pressure from free/cheap automation (Simplify Copilot free autofill/tracking/matches/resume builder; JobCopilot from $0.93/day; LoopCV free/€9.99+/month) | A generic autofill subscription cannot command a premium; volume claims encourage the wrong behavior | Keel Launch & Growth | Open (market condition, not a defect) | Demonstrable incremental value only (fewer unsuitable applications, less repeated input, supported execution, credible outcomes); no unlimited-volume promise until failure/support/usage distributions are known |
| E-I12 | Integrity fixes can reduce reported throughput (closing F17/F18/F22 removes false success and unsafe fallback) | Metrics will look worse before they are honest; stakeholders must not regress the gates to recover the number | Keel Program Office | Standing directive: treat the drop as measurement correction; report trustworthy completions only | Report trustworthy completions only |
| E-I13 | Fixed earlier findings F01–F16 exist only in a separate review copy — nothing is deployed | Original snapshot unchanged; reconstructed copy's signature change intentionally breaks old envelopes; pending releases need fresh gate review; shared signing-key access remains open (F32) | Keel Build | Partially fixed (review copy only) | Review and rebase the diff against the actual current repository; migrate approvals with fresh gate review |
| E-G1 | Gap-register G1 (prove application arrival) — PARTIAL_LOCAL_IMPLEMENTATION | Built: stable attempt/dispatch IDs, strict signed receipt writer, UNKNOWN recovery, legacy entry points guarded. Remaining: provider-specific evidence and reconciliation, browser/API entry-point migration, receipt-backed dashboard projections | Keel Build | Partial | Per the remaining list |
| E-G2 | Gap-register G2 (send exactly approved content) — PARTIAL_LOCAL_IMPLEMENTATION | Built: frozen attachment bytes and SHA-256, scoped public-key approval verification, provenance-preserving answer resolution, unhanded packet revision. Remaining: real authenticated review issuer, separate signing authority and key lifecycle, all answer consumers and adapter mappings | Keel Build | Partial | Per the remaining list |
| E-G3 | Gap-register G3 (maintain usable READY supply) — PARTIAL_LOCAL_IMPLEMENTATION | Built: canonical evaluator and disjoint inventory, transactional control revisions and claims, bounded demand-driven refill proposals. Remaining: production evidence publishers and queue adapters, scheduler leases and source/provider budgets, measured <1% idle and post-READY rejection gates | Keel Build | Partial | Per the remaining list |
| E-G4 | Gap-register G4 (independent-user journey) — SPECIFIED_NOT_IMPLEMENTED | Guided profile confirmation, saved search policies, application review, decision inbox, pause/resume, recoverable setup, 8/10 usability test — all unbuilt | Keel Launch & Growth | Not started | Build the journey; run the 8/10-in-15-minutes usability test |
| E-G5 | Gap-register G5 (measured route support) — PARTIAL_LOCAL_IMPLEMENTATION | Built: unique independently eligible coverage reducer. Remaining: supported provider/form matrix, source freshness and actual contract fixtures, visible account and handoff requirements | Keel Build | Partial | Per the remaining list |
| E-G6 | Gap-register G6 (evidence of better outcomes) — PARTIAL_LOCAL_IMPLEMENTATION | Built: 30/60-day complete-window cohort reporting, censoring and 95% Wilson intervals. Remaining: outcome consent and ingestion, independent relevance labels and calibration, held-out ranking evaluation, controlled comparisons | Keel Competitive Intel | Partial | Per the remaining list |
| E-G7 | Gap-register G7 (complete unit economics) — PARTIAL_LOCAL_IMPLEMENTATION | Built: attempt-attributed aggregation including failures, replay deduplication and missing-meter coverage, separate human recovery time. Remaining: meter instrumentation and durable event storage, infrastructure cost allocation, active user time and missing human-meter accounting | Keel Program Office | Partial | Per the remaining list |

## F. Cleared with tests (evidence the loop compounds — kept on record, not as open work)

- Launch-lock atomicity (O_CREAT|O_EXCL, 20-thread race test, pre-spawn duplicate guard, 2h fail-open expiry) — 9 tests green (2026-09-16).
- Atomic queue writes (flock + tmp+os.replace + patch_entry) — 20-thread patch storm, reentrancy, crash-safety tests green (2026-09-15).
- Stale-park guard false-positive repair (action-identity keying) — 4 files, 55 tests (2026-09-17).
- Answer-consistency guard parser defect repair (23 false-parked leads; 19 restored) — standing rule: when a guard parks N leads with an identical reason, suspect the guard first (2026-09-17).
- P0 answer-mismatch flood RESOLVED (2026-09-17): pre-fix guard misclassified the canonical `personally_completed_certification: NEEDS_INPUT` deferral as `out_of_scope`, parking 153 verified-live leads. Fix (deferral recognition via `_is_deferral_annotation`) verified: 152/153 archived flood packets CLEAN, 31/31 guard tests green. Targeted rescreen: **119 promoted to READY** (within the 90–135 expected range); 31 correctly remain parked on genuine Trent-input blockers (GENUINE_PAT audit); 3 ambiguous left parked. Boundary intact: the bank entry is explicitly "DO NOT CERTIFY" — no false attestation, no-AI/unaided-work boundary never weakened. Standing rule re-confirmed at scale: the guard was the defect, not the leads.
- Tray-answer compounding repair (answered questions re-parking; 15 leads promoted) — tests/test_tray_answer_compounding.py (10) (2026-09-17).
- Prescreen D1 numeric blind-spot fix — 5 new tests, 71/71 green (2026-09-17).
- needs_user zero-step stall detection + close/respawn plan — tests/test_parked_task_sweep_zero_step.py (7) (2026-09-17).
- Lane-stall post-mortem fixes (three-queue sweeper, parked-forever close proposals, main-chat delivery, repair-inflight fail-closed guards) — 23 regression tests green (2026-09-17).
- Verify-retry resurrection gate, GENUINE_PAT false-positive repair (8 leads unblocked), title-triage Kitsch repair (57 leads promoted), api_direct_detect over-claim closure, ledger append canonical writer (20 rows backfilled), outcome-listener triage repair (0 → 144 classifications), park verification + tripwire (2026-09-14..17).
- Keel transfer: 4 safe improvements cherry-picked and tested (log_event recursive scrub; gates.py disclosure/media_path; blocker.py prefix removal; preferences.py tightening + companion test) — 80 passed/5 pre-existing errors (input resolution), 27 passed (log-event), 25 passed (MUSE adversarial). Backups: ~/workspace/keel-transfer/cherry-pick-backup-20260917/ (2026-09-17).
- Bank-write → parked-lead sweeper (PQ-0A): every bank write sweeps parked leads with strict canonical matching only, revives fully-unblocked leads via canonical verify_retry — 126 blockers cleared across 94 leads, 0 no-AI/unaided-work boundary violations, READY 3 → 13; 16 regression tests green, queue backups taken. One dev incident (hook fired live during its own test run) audited clean and fixed; disclosed to Keel Build (2026-09-17).
- READY-starvation cooldown override (PQ-0B): REJECTED with measured evidence — root cause was parked leads with answerable blockers (fixed by PQ-0A), not cooldown mechanics; overriding would re-burn ~770 HTTP verifications with real 429 risk. Revisit only if READY<5 recurs with evidence cooldowns (not blockers/promotability) are the binding constraint (2026-09-17).
- Triage sieve tooling (P-2026-09-17-arm3-octopus-1): deterministic, read-only sieve on learning-proposals.md — 136 pending drafts → 73 promote-candidates, 45 review, 14 needs-evidence, 4 duplicate, 0 defer (fail-closed). Hard invariant: the tool never promotes (verified behaviorally + statically) — promotions stay human-approved. 18/18 tests green, neighboring charter suites 39/39 (2026-09-17).
- Renderer-verdict persistence + employer-hosted browser routing (PQ-9 / Build audit P1): full `api_direct_detect` verdict stamped at verify time; employer-hosted short-circuits before the api-direct guard lock — kills ~48 attempt-time `technique_blocked`/day and their lock churn. 163 tests green (6 new + 157 neighboring); ARM 70 seam review approved with GATE_TYPES registration fix applied (2026-09-17).
- Durable cross-process HTTP 429 host cooldowns (Keel 0.3.1 review K43/K44 port, 2026-09-17): new `engines/application-executor/host_cooldowns.py` — a 429 observed by any worker writes a durable per-host cooldown file (`hidden_files/http-cooldowns/`, POSIX flock, atomic tmp+fsync+replace); admission check fails fast before any request is issued, so the standing "429 = hard stop" rule survives process restarts and covers concurrent workers. Strict Retry-After parsing (integer seconds or HTTP-date, 60s floor, unrepresentable → indefinite hold with operator `clear_host()` escape hatch); corrupt/wrong-host/wrong-schema state denies admission fail-closed. Wired into `verify_retry_async._get` (production async path): pre-request admission check + post-429 record + semaphore/queue waits bounded by the caller's monotonic deadline (K43). 15/15 new tests green (12 module incl. cross-process fork test + 3 async wiring tests with fake session); 32 neighboring async/verify tests green. Pre-existing: 3 race-taxonomy tests fail under the venv python but pass under system python3 — environment-dependent, module imports only `verify_retry` (sync), untouched by this port.
- Prescreen fail-closed (Keel 0.3.1 review K06 port, 2026-09-18): prescreen exceptions no longer coerce to CLEAN — `_buffer_lead` parks with reason + archives via `prescreen.park_lead` (gate event through the standard park path; ARM-77 durable-gate mirror on park failure); parallel worker deletes only its own packet and reports skip. No raw raise — a prescreen bug degrades to parked leads, never a crashed pass or a CLEAN coercion. 3 new tests green, 483 neighboring green (packet_buffer, materials, buffer, cross-queue, eligibility suites). Backups: `apply_loop_backup-20260918-k06-prescreen-failclosed.py`, `parallel_packet_build_backup-20260918-k06-prescreen-failclosed.py`.
- Material-path containment (Keel 0.3.1 review K11 port, 2026-09-18): `_contained_material_path` (adapted `contained_path`) rejects absolute paths, `..` escapes, and external symlinks in `materials_readiness` (fail-closed → not-ready skip) and `build_packet` upload_files assembly (fail-closed → RuntimeError, no packet). Risk precondition passed: 941 material paths swept across standard/strategic/needs_input queues — zero strays. 15 new tests green, same 483-neighbor run green.
- Hash-based launch-lock filenames + fail-closed corrupt locks (Keel 0.3.1 review K19/K20 port, 2026-09-18): `_lock_path()` → `sha256(role_id).hexdigest()+".json"` (sanitizer collision class removed); 73 on-disk leases re-keyed by the one-shot drain (`drain_launch_locks_k19.py`, dry-run first), 0 orphaned, 9 fresh locks visible to both readers; `_read_lock()` now raises `ValueError` on malformed JSON / non-dict / role_id mismatch (silent stale-takeover removed) after a full caller audit found no silent-takeover dependency. `parked_task_sweep._fresh_lock` mirrored; Sept-17 manual release procedure verified path-agnostic (no doc change needed). 14 new tests (8 K19 + 6 K20), 63 neighbors green (x20, parked_task_sweep, inflight_marker).
- Cache hardening (Keel 0.3.1 review K21/K22 port, 2026-09-18): `fresh_live()` now requires `timedelta(0) <= age <= TTL` (future-dated stamps fail closed to HTTP re-check instead of short-circuiting claim-time verify); `_norm_url()` lowercases scheme/netloc only via urlsplit/urlunsplit (path/query case now significant; trailing-slash folding kept). 4 new tests, 124 neighbors green.
- Capture-load hardening (Keel 0.3.1 review K23 partial port, 2026-09-18): `load_capture()` enforces captured-flag, PDT-wall-clock age bound (2h, kwarg), realpath containment, filename==sha256, byte-count/size bounds, digest re-check; zero production callers verified by audit. `usable_for_liveness=False` deliberately NOT ported (conflicts with live 2026-09-16 design); age bound adapted for live's naive-PDT stamps. 6 new tests green.
- Telemetry credential scrubbing (Keel 0.3.1 review K24 port, 2026-09-18): Bearer + `key=value` string regexes added to `scrub()`'s string branch (key-drop style untouched). Risk precondition passed: 15,561 production telemetry rows / 139,207 string values scanned — exactly 2 matches, both benign employer-email unsubscribe tokens (redaction is a privacy improvement; no live consumer reads them). 4 new tests green.
- Replay-safe event IDs (Keel 0.3.1 review K25 port, 2026-09-18): optional `event_id` on `log_event.log()` with a bounded 256 KiB tail replay-check integrated with the live hash chain (not the candidate's plain append); threaded through `record_outcome.record()` (technique-library write tagged + deduped — the library half is fully handled, not partial) and `worker-charter/ingest_envelope.py` (`event_id="envelope:"+sha256(env)+":"+i`); conflict → `ValueError` before any write. 6 new tests green, 258 neighboring log_event/record_outcome tests green.
- Strict outcome validation (Keel 0.3.1 review K26 port, 2026-09-18): `math.isfinite` + 0–100 bounds + `datetime.strptime` calendar-date validation; bool fit_score now hard `ValueError` (was silent drop). Risk precondition passed first: 2,865 numeric fit_scores across all three queues all in range (min 30 / max 94); `apply_loop.py:4434` producer audited clean. 4 new tests green; `test_scoring_persistence` updated to the new bool contract.
- Dashboard HTML injection + unknown-not-zero + import-safe (Keel 0.3.1 review K31/K32/K33 port, 2026-09-18): `fmt_ts` exception path escaped + restrictive CSP (`default-src 'none'`) on both dashboards (job-pipeline + keel); missing/corrupt queue or ledger now renders "Unknown" + banner, never healthy zero; collect/render/build/main split with `__main__` guard (import performs zero dashboard I/O). Refactor precondition: byte-identical HTML proven on clean data (live data + fixture). 30 new tests green (10 injection + 20 unknown/import-safe), neighbors 24/25 (1 pre-existing data-drift failure in untouched `test_repost_watch_parser.py`).
- Reproducible packager (Keel 0.3.1 review K36/K37 port, 2026-09-18): `~/workspace/keel/package.sh` now includes `monitors/` + `worker-charter/` in payload, uses `mktemp -d` staging with EXIT trap (no more `rm -rf /tmp/keel-pkg`), fixed mtimes (2020-01-01), sorted entries, normalized modes, per-file SHA-256 MANIFEST.json, exclusive-open output refusal, post-build verify, `--verify ZIP` mode. Byte-identical builds proven (two builds → `09e07917f1…51df7` == same); personal-data scrub preserved verbatim (three-way verification). 9 new tests green, 115 keel-suite green.
- Durable queue writes (Keel 0.3.1 review K38 port, 2026-09-18): `queue_io.atomic_write_json` now uses mkstemp temps, file fsync before rename, directory fsync after, 0600 mode, temp cleanup on failure (flock half untouched); mirrored to `~/workspace/keel/engines/queue_io.py`. Latency measured before enabling: +35 ms/write (~1.3% of the async-verify budget — not material). Side effect: queue files now commit at 0600 (was 0644) — same-user tooling unaffected. 5 new tests green, test_queue_io 25/25.
- Strict JSON loads (Keel 0.3.1 review K50 port, 2026-09-18, strict-JSON half only): new `queue_io.strict_loads` rejects duplicate keys, non-finite values, and overflows; routed through `load_json`, `_read_meta`, `apply_loop._read_claim_packet_json`, `answer_resolver.load_bank`. Dry-run audit first: all 10 genuinely-live JSON files clean; 2874-file sweep found only 3 non-JSON `.json` archive artifacts (never loaded). Integer-version component excluded per triage. 4 new tests green; 105 + 27 + 45 neighboring suites green.
- SSRF/redirect policy layer (Keel 0.3.1 review K41 port, 2026-09-18): new `engines/application-executor/http_policy.py` — `validate_url` + `_public` + `resolve_public` as pre-request admission (bounded DNS round-trip, non-public destinations refused; plain `http://` admitted since the verifier checks plain-http posting pages). Wired into `http_cache._network_get` (per-hop redirect re-admission), async `_get` (manual 3-hop redirect loop), and sync `check_live`/`fetch_posting_text`. Policy refusals subclass `URLError` (not `HTTPError`) so 404/410→dead semantics are untouched. Sync paths also consult `host_cooldowns` (admission check + record_429) — closes the K44 sync-path caveat. Raw-socket exchange NOT ported (proxy-incompatible). 18 new tests green, 33 neighbors green.
- HTTP body + redirect caps (Keel 0.3.1 review K43 port, 2026-09-18, body/redirect halves): 4 MiB body cap + max 3 redirect hops on `http_cache._network_get` and async `_get`. Largest legitimate board-API payload measured 1.5 MiB (Ashby Headway — 2.7× headroom). 11 new tests green, same 33-neighbor run green.
- Adversarial-review unavailability blocks ship (Keel 0.3.1 review K45 port, 2026-09-18): silent unavailable paths (unreadable diff, vacuous entry, reviewer produced no report — missing CLI, crash, hang, nonzero-exit-with-empty-stdout) now return `(False,…)` and persist an `adversarial-blocked` history record naming the remedy. Reviewer-reported `unavailable` (valid JSON) keeps the documented non-blocking contract. 7 new tests green; blackboard/fanout_gate/pulse suites green.
- Outcome-linking strictness (Keel 0.3.1 review K48 port, 2026-09-18): ambiguous company-only events and unparseable timestamps now stay UNLINKED (live `norm_company`, `LINKABLE_STATUSES`, `(linked, unlinked)` shape preserved). Measured on real data: unlinked_rate 0.135 → 0.917 — dominated by RFC-2822 Gmail timestamps `parse_ts` cannot parse (pre-existing gap; zero true-ambiguity cases in current data). Follow-up: extend `parse_ts` to RFC-2822 as a separate tested fix (~122 events recoverable). 11 new tests green; test_linking 10/10, evidence_gate 27/27.
- Passive form-inspection module (Keel 0.3.1 review K49, module landed / wiring BLOCKED, 2026-09-18): new stdlib-only, read-only `html_form.py` (fail-closed caps, `extraction_complete: False`) + 5 regression tests incl. a pinning test that fails loudly if wired without the fix. Probe_url wiring BLOCKED with evidence: prescreen renders intel as "VERIFIED PRE-LAUNCH" and parks leads on partial labels — wiring now would park clean leads. Safe wiring needs a distinct non-authoritative intel rendering first.
- K48 follow-up SHIPPED (2026-09-18): `parse_ts` now handles RFC-2822 via stdlib `email.utils.parsedate_to_datetime` — measured on live telemetry, 139/139 Gmail `message_date` values parseable (were 0), so the ~122-event recovery materialized. Also fixed a pre-existing tolerant-parser bug the tests caught: the ledger-style branch's `strptime` could raise `ValueError` on impossible dates (e.g. 2026-13-99) instead of returning None. 7 new tests green; 109 outcome-tracking + linking suites green.
- K49 advisory wiring SHIPPED (2026-09-18): the wiring block is resolved. `form_intel.probe_url`'s unknown-ATS fallback now runs passive inspection (via the policy-wired `http_cache.fetch`), but passive labels ride under `intel['passive_intel']` with `advisory: True` while `questions` stays `[]` — the authoritative contract is unchanged. New explicit guard `form_intel.is_authoritative_intel` / `authoritative_questions`, enforced at all three gating consumers: prescreen `render_probe_brief`, brief_builder's FORM INTEL section, and apply_loop `_form_questions` (T4 bundle form_fingerprint). Proven end to end: identical blocking labels (essay + no-AI attestation) PARK as authoritative but return CLEAN as advisory; passive labels appear in no brief, no manifest. 12 new tests green; 45 prescreen + 33 brief/bundle neighbors green.
- Keel mirror ports K21/K22/K23 (2026-09-18): `~/workspace/keel/engines/` mirrors of `live_cache.py`/`page_capture.py` carried the same defects — ported the non-negative age bound, case-preserving URL norm, and fail-closed capture integrity (containment/hash/size/PDT age bound, 4 MiB cap), adapted to the mirror's `keel_paths` layout. 12 new tests green; full keel suite 192 green.
- Shadow readiness evaluator (Keel 0.3.1 review K51 narrow port, 2026-09-18): new `readiness_shadow.py` — `evaluate_readiness`, `dependency_hash`, typed shortage states (`EXECUTABLE`/`AWAITING_DECISION`/`PROVIDER_LIMITED`/`INTERNAL_FAILURE`) with a live-queue-entry schema adapter; shadow-mode ONLY (AST-enforced no-launch/no-write, B-D9 referenced in docstring). `operations.py` not ported (redundant with live sweeps). Read-only CLI against the real queue: 103 nominal READY → 4 shadow-executable with typed shortages. 44 new tests + 25 subtests green, 122 neighbors green.
- Metric existence gate (Keel 0.3.1 review K61 tier A port, 2026-09-18): `--metric` must now resolve to a real optimization-log record (union of both live logs); invented labels refused. Tier B (finite-baseline/hash chain) stays unported pending log-schema migration. 32 unlogged post-gate labels refused by design (log-first contract; refusal prints the exact `log_optimization.py` command). 6 new tests green.
- Watermark physical-line cursor (Keel 0.3.1 review K47 test-first port, 2026-09-18): regression test written FIRST (failed pre-port: watermark 3 vs 5 physical lines), then the one-liner (`new_wm = i + 1`) — malformed telemetry lines now advance the watermark instead of causing permanent reprocessing. 4 new tests green, 7 pre-existing mine_learnings tests green.

---

## Source-arbitration notes (do not resolve unilaterally)
1. "36h without discovery sweeps" and "Greenhouse early dedupe" appear in pipeline pulse reports, not in AGENTS.md. Pulse 316 reported ~36h without sweeps and Greenhouse JSON as the largest re-harvest source (456/1,652 staging events) without an early dedupe check — captured in B-F2.
2. "RL-3 verification backlog" appears in the lane watchdog output (607), not in AGENTS.md or MEMORY.md. Captured via pulse 318/watchdog in the live snapshot.
3. "Release-on-refuse / lock-on-refuse" as a named defect is pulse-side language; the AGENTS.md mechanic (stale 2h locks after fastlane_browser refusal + manual refill) is captured in B-E1.
4. ~~The standing answer-consistency dispute (J-20260917-1356-park-1449) is owned by the main agent for adjudication — surfaced but not re-adjudicated here.~~ **Resolved 2026-09-17 on measured evidence:** Pipeline Performance's canonical rescreen (119/153 promoted READY, 31 genuine parked, 3 ambiguous; 152/153 packets CLEAN, 31/31 guard tests green; no-AI boundary intact with DO NOT CERTIFY) adjudicates the dispute. Main agent retains reopen rights on new evidence.
5. Pulse ledger counts 195 vs 196 — ledger semantics must be reconciled before quoting a definitive submitted total.
6. Correction 2026-09-17: `conflict-review.md`'s "5 live Stripe api-direct submissions tonight" premise is not telemetry-supported (0 Stripe api-direct submissions 09-15→17 — the 3 Stripe submissions that day were browser-lane with confirmation_text). The kill-switch rejection stands on its architectural merits regardless; A-C1's evidence corrected.

## Counts
- Open P0: 41 (A: 18 open — 21 rows include 3 rejected/superseded decisions recorded, not open work; B: 23) · Open P1: 21 · Open P2: 31 (D: 16, E: 15). **Total open/partial: 93.**
- Cleared-with-tests line items: 15 (evidence of compounding).
- Gap-register G1–G8: 7 partial, 1 specified-not-implemented; production_deployed: false.

**Register protocol:** new limitations get an ID, evidence link, and impact before anyone claims work on them. A limitation is CLEARED only when its acceptance evidence lands in this file. Sidebar rule: any path-to-clear that requires funds goes to funded-sidebar.md and is marked here as `SIDEBARED`.
