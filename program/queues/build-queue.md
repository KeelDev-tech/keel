# Build Department — Standing Work Queue

**Owner:** Keel Build department. **Rule:** finish an item → pull the next yourself. Below 5 queued → refill from the limitations register by priority AND run your own gap-hunt. Never wait for direction — the queue is the direction. Zero queued items = P0.

🔒 = integrity-gated (ship only on evidence/test gates; no false closure).

| # | Item | Source | Integrity | Status |
|---|---|---|---|---|
| B-01 | Receipt-writer adversarial re-verification (rebuild + independent re-verify the writer; FAIL was on the receipt writer, not the loop) | Register A-C2 | 🔒 | QUEUED |
| B-02 | F17: provider/attempt-correlated confirmation contract; ambiguous 2xx stays UNKNOWN | Register F17 | 🔒 | QUEUED |
| B-03 | F18: final evidence writer accepts only verified receipt references (not caller flags) | Register F18 | 🔒 | QUEUED |
| B-04 | F22: one durable attempt shared by API and browser lanes; no retry/transport switch until authoritative reconciliation | Register F22 | 🔒 | QUEUED |
| B-05 | F20: immutable submission bundles (attachment SHA-256/size/type, target identity, exact answers, form version) | Register F20 | 🔒 | QUEUED |
| B-06 | F21: authenticated approval records bound to principal, scope, expiry, exact bundle | Register F21 | 🔒 | QUEUED |
| B-07 | F34: one context-aware answer resolver used by every consumer (scope/source/expiry; abstain on inapplicable scope) | Register F34 | 🔒 | WIRED 2026-09-19 — resolve_key() is the single F34 consumer entry point on typed_resolver (bank-entry adapter with explicit status derivation; value returned ONLY on OK). 12/12 conformance tests green. resolve_shadow.py migration gate: zero DIVERGE on the live bank (shadow caught 2 real defects pre-wire: scope_covers exact-match bypass on non-preauthorized attestation; preauth standing keys failing closed as legacy — both fixed + regression-tested). Consumer #1 wired: tray_inbox bank annotation via resolve_key (live: 1 AUTO_FILLABLE card citing quarantined `location` now honestly reported, not counted). 105 keys on the type-registration worklist (UNCLASSIFIED, fail closed). Remaining consumers (brief_builder, prescreen, field_question_protocol, input_broker, tray_answer) migrate at zero DIVERGE. |
| B-08 | Reconciliation design phases 0–2 (receipt archive D1, gateway interface D2, UNKNOWN reconciler D3) on test gates | Build reconciliation-design.md | 🔒 | QUEUED |
| B-09 | Reconciliation design Phase 3 (holds/UNKNOWN/READY) — needs Pipeline Performance sign-off before any behavior change | Build reconciliation-design.md | 🔒 | QUEUED — blocked on sign-off |
| B-10 | P2 embed-endpoint fallback per-employer contract — needs Trent approval (PD-5) | Renderer audit P2 | 🔒 | QUEUED — Trent-gated |
| B-11 | A-B receipt-verifier per route (validated success-receipt parsers; provider-specific evidence contracts) | Register A-B1/A-B7 | 🔒 | QUEUED |
| B-12 | A-B hold system on canonical application identity + authenticated reconciliation mechanism | Register A-B2/A-B5 | 🔒 | QUEUED |
| B-13 | Trust perimeter: F27–F33 blockers' hosting prerequisites (transactional boundary, audit chain, tenant scoping, recoverable setup, deletion/export accounting) | Register F27–F33 | 🔒 | QUEUED |
| B-14 | Cost metering: per-submission HTTP attribution + tap signal; attempt-level metering toward real dollars | Register D-H5 / T3 | — | QUEUED |
| B-15 | D-H2: attestation scope in spawn briefs (only the five pre-authorized keys checkable; anything else parks Trent-only) | Register D-H2 | 🔒 | QUEUED |
| B-16 | **Remediation port 1 — launch_lock serialization (B-E1):** port `patches/launch_lock.py.patch` diff-first against live `engines/application-executor/` — `_serialized` per-path flock on acquire/release, fail-closed freshness, corrupt-lock `HELD/corrupt_lock_requires_inspection` refusal, fsync on writes. Base MATCHES live. Coordinate deployment so no mixed lock versions; manual `_released/` file-move procedure still valid; never bulk-delete old locks | No-service remediation package 2026-09-17 (verified review dir `keel/remediation/no-service-2026-09-17/review/`) | 🔒 | QUEUED |
| B-17 | **Remediation port 2 — api_direct_loop claim tokens + pre-dispatch release (B-E1):** per-invocation claim tokens (`api-direct-loop:` + uuid4, fixes live fixed-`TASK_ID` cross-run lock inheritance) + `release_pre_dispatch_lock` on grant/staging/telemetry failure + post-dispatch lease retention. EXCLUDE the renderer hunk (would regress the live P1 employer-hosted renderer refusal; `browser_handoff` consumer does not exist). MERGE the guard parser keeping the live legacy last-line fallback + exception detail, adding `holder=` reporting and stricter `status=="ACQUIRED" and verdict=="GO"` acceptance | No-service remediation package 2026-09-17 | 🔒 | QUEUED |
| B-18 | **Remediation port 3 — fastlane preflight + pre-dispatch reorder (B-E1/B-E2):** `fastlane_screen(stage=False)` preflight-only mode; live currently stages before lock ownership. REQUIRES decision on `preparation_request` semantics FIRST: build the parent-controller consumer or park the request-stamping. Keep live board-stat vocabulary (`skipped`/`no_packet`) | No-service remediation package 2026-09-17 | 🔒 | QUEUED — decision-gated |
| B-19 | **Remediation port 4 — queue_io primitives + parking_schema fix (D-H12/D-H11/F27 partial, D-H14):** port fsync (file+directory) in `atomic_write_json`, `_document_entries` wrapper preservation, per-call `lock_path=` — must COEXIST with live `set_lock_path()` global-redirect (different design). Pair with the small `parking_schema` list-note fix (base MATCHES live). EXCLUDE `park_entry` as-is: stamps `NEEDS_INPUT` vs canonical `PARKED-NEEDS-INPUT`, skips cross-queue move + C-19 verification | No-service remediation package 2026-09-17 | 🔒 | QUEUED |
| B-20 | **Remediation observers — shadow-only:** `readiness.py` refill/readiness diagnostics, `measurement.py` missing-coverage reporting (cost instrumentation STAYS AN OPEN GAP — do not close), `discovery.py` read-only Greenhouse staging behind the existing scheduler, `answers.py` shadow evaluator ONLY after an adapter maps the flat live `answer_bank.json` schema (needs `question_hash`/`expires_at`/`scope`/`source_ref`/`attestation_key`). Never wire to dashboards/claims before live adapters publish authoritative observations | No-service remediation package 2026-09-17 | 🔒 | QUEUED |
| B-21 | **Remediation sidecar — journal.py (diagnostic only, lowest priority):** diagnostic sidecar, NEVER the live transaction boundary; never point at the live DB. A-B6/F27/F19/F26 stay open | No-service remediation package 2026-09-17 | 🔒 | QUEUED |
| B-22 | **G-1 — user-facing recurring run object:** one named persistent run object per user (schedule/pause/resume, visible run history derived ONLY from the append-only telemetry log, user-tightenable launch limits, stable F22 attempt IDs). Full acceptance criteria in specs/keel-05-gap-acceptance-criteria.md §G-1 | Teardowns G-1 / spec-matrix §5 | 🔒 | QUEUED — tested prototype at program/prototypes/run-history/ (6/6 tests: exact rows, honest zero-activity, ambiguous isolation, pause/resume no-double-count, malformed surfaced) |
| B-23 | **G-2 — designed execution escape hatch:** assisted-completion workflow for unsupported routes — one honest visible state per blocked lead (bounded assisted action, Trent-tap fallback where policy requires), attribution of automation-vs-user completion through receipts. Full AC in specs/keel-05-gap-acceptance-criteria.md §G-2 | Teardowns G-2 / spec-matrix §5 | 🔒 | QUEUED — tested prototype at program/prototypes/escape-hatch/ (9/9 tests: total router over 2000-lead fuzz, Trent-only→TRAY with no workarounds, CAPTCHA cap, actor-split receipt labels) |
| B-24 | **G-3 — productized blocker inbox:** the Tray as a first-class decision surface — one inbox with gate citations, recurrence view, answer provenance (his-words vs suggested), retro-resolution with honest unblock counts. Full AC in specs/keel-05-gap-acceptance-criteria.md §G-3 | Teardowns G-3 / spec-matrix §5 | 🔒 | QUEUED — tested prototype at program/prototypes/blocker-inbox/ (8/8 tests: dedupe cards, suggested-never-banked, provenance trace, exact retro-resolution, rejection leaves open) |
| B-25 | **G-8 — user-facing volume controls:** bounded-aggression config, saved-search policies, visible launch limits; user settings tighten only, never exceed operator caps. Acceptance: user can configure aggression + limits unaided; replay proves limits are hard ceilings (no overrun on a 500-lead stress run) | Teardowns G-8 / spec-matrix §5 | 🔒 | QUEUED — tested prototype at program/prototypes/volume-controls/ (11/11 tests: clamps recorded not silent, 500-attempt stress 50 launched/450 denied/zero overrun, caps immutable) |
| B-26 | **G-4 — application-to-interview continuity:** post-submission tracker surface (SUBMITTED → REJECTED/WITHDRAWN/INTERVIEW/OFFER/HIRED/EXPIRED-UNKNOWN), outcome ingestion on authorized evidence only, tracker feeds CI-04 cohorts. Full AC in specs/keel-05-gap-acceptance-criteria.md §G-4; depends on CI-04 + G1 privacy-counsel sign-off | Teardowns G-4 / spec-matrix §5 | 🔒 | QUEUED — tested prototype at program/prototypes/interview-tracker/ (7/7 tests: 500-submission 1:1 ledger reconcile, no-inference state changes, fail-closed signals, EXPIRED-UNKNOWN cohorts) — depends on CI-04, G1-gated |

**In progress:** none open beyond PQ-9 support (shipped 12:59 PDT).
**Completed this week:** triage sieve tooling (18/18 tests), renderer-audit P1 review + ARM 70 seam review, conflict-review correction, cherry-picked transfer improvements (4 safe, tested).

Depth: 26 queued · 0 in progress · 4 completed (2026-09-19 02:47 PDT).

**Standing guardrails on the remediation ports (B-16–B-21) — non-negotiable:**
- `production_deployed` stays false everywhere. No gateway restoration: the older rejected gateway/cumulative patch (`changes-from-original_13_y72x.patch` `keel_core/gateway.py` bundle, `changes-from-prior-review_12_jf9a.patch`) stays excluded — the new package contains only do-not-restore instructions (A-C1/A-C4/A-C5).
- All ports are isolated diff-first against live code; never overwrite newer live source, never blind-apply. The package's 122-test figure is a source claim (82/82 independently reproduced; `--executor` variant was correctly refused by the stager on the diverged live tree) — not a release approval, audit, or benchmark.
- Receipt truth: `receipt_diagnostic` stays UNKNOWN-always; A-C2/A-C3 receipt-writer hold stays shadow-only until provider contracts + adversarial gates pass. No SUBMITTED-writer changes until the receipt contract exists (D-H6 open).
- `remediation-status.json`: 100 entries, **0 cleared by this delivery**; the 93 open/partial headline is a source claim, not a reconciled live count.
- 429 = hard stop. Per-invocation tokens, never bulk stale-lock deletion. RP01, PQ-0A/PQ-9, submission-truth controls, and full-autopilot attestation scope preserved. Main-chat-only browser lane untouched.
- No spend, no outreach, no reference contact, no invented claims.
